package de.eldoria.bloodnight.config.worldsettings.mobsettings;

public enum VanillaDropMode {
    /**
     * Drop only vanilla drops
     */
    VANILLA,
    /**
     * Combine custom and vanilla drops
     */
    COMBINE,
    /**
     * Drop only custom drops
     */
    CUSTOM
}
